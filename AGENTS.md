# Website workflow

After each completed user-requested website change, run the appropriate checks,
commit the change, and push the current branch to origin. The user has requested
this as the default workflow.
